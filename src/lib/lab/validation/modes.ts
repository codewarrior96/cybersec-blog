import type { ValidationMode } from './types'

export const challengeModes: Record<number, ValidationMode> = {
  1: 'hybrid',
  2: 'legacy_flag_only',
  3: 'legacy_flag_only',
  4: 'legacy_flag_only',
  5: 'legacy_flag_only',
  6: 'legacy_flag_only',
}
